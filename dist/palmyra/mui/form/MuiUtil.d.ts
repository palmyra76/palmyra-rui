import { AttributeDefinition } from "../../form/interface";
declare const copyMuiOptions: (props: AttributeDefinition, value: any) => {
    disabled: boolean;
    required: boolean;
    placeholder: string;
    value: any;
};
export { copyMuiOptions };
