import { FieldDefinition } from "../form/Definitions";


class DateTimeValidator {

    validate = (format: FieldDefinition) => {
        // Mandatory, DatetimeRange
        // Typed Validator: dateTime  
    }
}

export default DateTimeValidator;