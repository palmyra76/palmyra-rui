import { FieldDefinition } from "../form/Definitions";


class DateValidator {

    
    validate = (format: FieldDefinition) => {
        // Mandatory, DateRange
        // Typed Validator: date
    }
}

export default DateValidator;