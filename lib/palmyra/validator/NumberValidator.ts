import { FieldDefinition } from "../form/Definitions";


class NumberValidator {

    validate = (format: FieldDefinition) => {
        // Mandatory, Range, 
        // Typed Validator: number, float
    }
}

export default NumberValidator;